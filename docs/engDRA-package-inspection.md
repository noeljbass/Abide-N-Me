# engDRA package inspection

The verified archive has SHA-256 `b891705afb54aac46b98d9b87320620fde7cab9cf6d1fc5459d2785d2df5cd94`, 76 entries, and 18,496,927 uncompressed bytes. The authoritative exact filename, byte-size, book, chapter, and verse inventory is generated by `php bin/import-bible.php --validate-only` in `storage/reports/engDRA-validation.json`.

It contains 73 UTF-8 `.usfm` files plus `copr.htm`, `keys.asc`, and `gentium.css`. Files have no explicit `\\usfm` declaration. Observed markers are `id`, `h`, `toc1`–`toc3`, `mt1`–`mt3`, `c`, `v`, `p`, `q1`, `cl`, and `w`; `w` spans carry Strong's attributes and are reduced to their displayed word during import.

The package uses standard USFM ids, with Douay names expressed in headings rather than alternative ids. `1SA`/`2SA`/`1KI`/`2KI` correspond to Douay 1–4 Kings; `EZR` and `NEH` correspond to Douay 1–2 Esdras. Tobit, Judith, Wisdom, Sirach, Baruch, 1 Maccabees, and 2 Maccabees are standalone books. Catholic Esther is integrated into 16 chapters. Catholic Daniel is integrated into 14 chapters, including Daniel 3:24–90 and chapters 13–14. Psalms contains 150 provider-numbered chapters and is retained using the source's Douay/Vulgate numbering rather than silently remapped.
