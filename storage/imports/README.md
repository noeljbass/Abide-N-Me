# Local Bible imports

Place the downloaded `engDRA_usfm.zip` package in this directory for the future
command-line importer. Import archives are ignored by Git and must not be served
or committed. On production hosting, keep the package outside the public web
root whenever possible and delete it after a verified import.
